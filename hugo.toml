baseURL = 'https://juery.fr/'
locale = 'en'
title = 'juery.fr'
theme = 'modeline'
enableRobotsTXT = true
enableEmoji = true
capitalizeListTitles = false

[pagination]
  pagerSize = 8

[taxonomies]
  tag = 'tags'

[outputs]
  home = ['html', 'rss', 'json']

[markup]
  [markup.goldmark.renderer]
    unsafe = true
  [markup.highlight]
    noClasses = false
    lineNos = false
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 3

[menus]
  [[menus.main]]
    name = 'Posts'
    pageRef = '/posts'
    weight = 10
  [[menus.main]]
    name = 'Tags'
    pageRef = '/tags'
    weight = 20
  [[menus.main]]
    name = 'About'
    pageRef = '/about'
    weight = 30

[params]
  description = 'Field notes from the terminal — editors, systems, and the craft of shipping.'
  copyright = 'juery.fr'
  footerNote = 'built with Hugo · :wq'

  # Header logo, rendered as {logoText}{logoAccent} with the accent colored.
  logoText = 'juery'
  logoAccent = '.fr'

  # Home hero
  heroEyebrow = 'Backend engineer · writing weekly'
  heroTitle = 'Editors, systems, and the craft of shipping.'
  heroText = 'Field notes from the terminal — Vim workflows, Go patterns, and honest takes on the tools we build software with.'

  # Features
  search = true            # client-side search + "/" shortcut
  loadGoogleFonts = true   # set false to use system fonts (or self-host)

  # Optional: pin tags to a pill color (blue, green, yellow, purple, red).
  # Unlisted tags get a stable color derived from their name.
  [params.tagColors]
    vim = 'blue'
    go = 'green'
    ops = 'yellow'
    rust = 'purple'
    rant = 'red'
